{
  "name": "ecowarriors-fullstack",
  "version": "1.0.0",
  "description": "EcoWarriors: Trash Toss Titans — Fullstack MERN Application",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd backend && npm install",
    "install-root": "npm install && cd backend && npm install"
  }
}
